# Work Log

## 2026-09-11

- Rebased the pending Focus Timer visual-reference work onto the latest public `main`, retaining the repository's canonical Studio import root at `skills/rokid-aiui-agent/assets/focus-timer-agent/`.
- Added a deterministic builder, a 2400×1200 GitHub user-journey PNG, and a six-page developer PDF covering voice setup, default 10 minutes, `_current`, `_blank`, nod/touchpad mapping, automated evidence, and manual gates.
- Added source-alignment and output-contract tests. The new test first failed because the builder was absent, then passed after the builder was added; regenerated outputs matched the checked-in PNG and PDF byte-for-byte.
- Closed a pre-publication Linux CI gap with pinned Pillow, ReportLab, and pypdf dependencies plus an installed and explicitly discovered Noto CJK font; the corresponding contract failed before the environment fix and passed afterward.
- Visually inspected the journey PNG and all six rendered PDF pages. Both explicitly identify themselves as illustrations rather than physical-device captures.
- Ran the final local publication gate on the remote-main-based worktree: 279/279 tests passed, Skill and reference validation passed, all four strict import-root checks passed, and Python/Bash/YAML plus full-tree whitespace checks passed.
- Probed AIX 0.8.2 help before execution, then ran its advertised preview, pack, and list commands for the Focus Timer. Preview produced 39,180 bytes; pack produced 19,232 bytes with SHA-256 `775a755ec33946fb19b63699ed5e396056d2d112b85ab0da196099514152b9a3`; list contained only the expected Agent, claims ledger, manifest/version, app files, and Page. All three repository AIX smoke flows also passed.

## 2026-09-07

- Confirmed the workspace was empty and not yet a Git repository; the expected continuity files did not exist.
- Confirmed GitHub CLI authentication for `BreezeLife` and verified that `BreezeLife/rokid-aiui-agent-skill` did not already exist.
- Initialized a local Git repository with `main` as the initial branch.
- Cloned source repositories into a temporary research directory without modifying upstream sources.
- Verified that `jsar-project/AIUI` and `jsar-project/aix` redirect to the `yodaos-project` organization.
- Found that the old AIX documentation hostname is stale and began validating commands against repository source and published packages.
- Downloaded the official nine-page ROKID AIUI courseware PDF, extracted its text, rendered it, and visually reviewed the relevant pages.
- Selected a compact router plus on-demand references and deterministic validation as the implementation direction.
- Found material conflicts inside the official AIUI repository: the current quickstart permits conversation-embedded interaction, v0.18 documents animation support, and the beta green design has moved to sparse 1px/open-field styling, while the bundled `aiui-dev` Skill retains older restrictions and visual tokens.
- Updated the design source precedence so current changelog/documentation, implementation, and runnable samples outrank stale bundled summaries.
- Completed the official design and third-party workflow audit. Recorded the unresolved distinction between the `480x352` Ink preview viewport and the official `480x640` optical canvas with a `480x400` preferred region.
- Ran three fresh pre-Skill scenarios. The baseline scored 7/30 with four critical failures: it removed a currently supported conversation interaction, invented AIX commands, and lacked a safe end-to-end release gate.
- Added a fourth Studio-delivery baseline. The complete RED suite scored 8/40 with six critical failures, including snippets-only delivery, invented project files/APIs, and a fake ZIP-to-AIX workflow.
- Implemented a compact `rokid-aiui-agent` Skill derived from the official `aiui-dev` routing model, with seven focused, commit-pinned references and an explicit stable `0.17.0` default.
- Packaged the Skill at `skills/rokid-aiui-agent/` so its directory matches frontmatter `name` and both GitHub CLI and generic Skills CLI can discover it.
- Added a complete Page-only AIUI source project under `assets/studio-importable-minimal/`; its project root is suitable for Studio local selection or GitHub specified-directory import.
- Built a dependency-free project validator with stable diagnostics for manifests, routes, `.ink`, WXML, Widgets, Agent Workers, application entries, version gates, and unsafe paths.
- Added adversarial regressions for empty or malformed WXML, crossed/unbalanced Ink tags, invalid `app.ink`, missing Widget family, near-match AIX listings, escaped/symlinked Markdown paths, and stale/unpinned source syntax.
- Checked the markup stack against all 12 official v0.17 sample WXML files and five representative official Ink Page/Widget sources without false positives.
- Verified the final suite at 49/49 unit tests, OpenAI Skill validation, GitHub Skill dry-run discovery, strict project validation, reference validation, Python compilation, Bash syntax, YAML parsing, and whitespace checks.
- Ran published `@yodaos-pkg/aix-cli@0.8.2` against both the fixture and packaged Studio example. Both produced non-empty archives whose exact listings contain generated manifest, `app.json`, and the declared Page.
- Generated a static AIX browser preview for the packaged example and visually confirmed `Preview ready` plus the expected “AIUI project ready” content. This remains browser evidence, not Studio or glasses evidence.
- Installed the Skill to isolated temporary directories with GitHub CLI, discovered it with the generic Skills CLI, and successfully ran the installed copy's bundled AIX smoke flow.
- Ran four fresh forward scenarios with the Skill. Results improved from 8/40 to 40/40 with no critical failures; the Studio scenario was rerun after adding an explicit prohibition on fabricated verification claims.
- Completed independent Studio-contract and adversarial code/CI reviews. Fixed the discovered selector, validator, reference, AIX listing, and pinned-CI execution issues; final reviewers reported no remaining implementation P0/P1.
- Created the public `BreezeLife/rokid-aiui-agent-skill` repository, pushed `main`, and verified its public visibility, default branch, homepage, topics, and published Skill/example paths through the GitHub API.
- Confirmed GitHub Actions run `34138056856` succeeded at commit `bb33dbe4e907aa1fe617690431d60e635c485886`, including both the validation job and the published-AIX packaging job.
- Reinstalled the published repository through both documented paths. `gh skill install` resolved the public `main` branch and copied the complete package; `npx skills add` resolved the same commit, copied all 16 Skill files without a diff, and the installed validator accepted its bundled Studio example in strict `0.17.0` mode.
- Re-ran `gh skill publish . --dry-run` after publication; discovery passed. The authenticated AIUI Studio import and physical-glasses checks remain explicitly separate manual gates.
- During version-matched source review, found that the Skill's Page definition example omitted the official `schema.data` envelope. Scoped a regression-first correction before generating the new Agent.

## 2026-09-08

- Added a regression for the official Page `schema.data` envelope and corrected the routed Ink authoring reference.
- Pinned both verification jobs to Node.js `24.19.0` and locked `@yodaos-pkg/aix-cli@0.8.2` through `package-lock.json`; local installation used `npm ci`.
- The lockfile-resolved AIX pack/list run produced a `10939`-byte archive containing exactly `AGENTS.md`, `META-INF/aix/manifest.json`, `VERSION`, `app.js`, `app.json`, and `pages/index/index.ink`.
- The lockfile-resolved AIX preview produced `28338` bytes and contained the exact Page path `pages/index/index.ink`.
- This is browser evidence only; it does not prove authenticated AIUI Studio import or physical-device behavior.
- Re-ran the final local gate from the feature branch: all 56 unit tests passed; all four stable/preview fixture and example strict validators exited cleanly; reference and OpenAI Skill validation passed; Bash syntax, both YAML files, and tracked whitespace passed.
- Reinstalled the lockfile with Node.js `24.19.0` and ran all three AIX pack/list gates successfully. The first local attempt exposed an unhealthy Homebrew Node selected by `/usr/bin/env`; explicitly placing the verified Node runtime first on `PATH` fixed the environment, and the complete gate was rerun rather than treating the first attempt as evidence.
- Final delivery review found that reference validation re-scanned Markdown inside installed dependencies. Added a RED/GREEN regression and pruned `.git`, `.worktrees`, and `node_modules` during traversal so the documented local workflow is repeatable after `npm ci` and in repository worktrees.
- Re-ran the complete suite after the review fix: all 57 unit tests passed, including the real Page state-machine harness and the installed-dependency reference regression.
- Recovered the exact verified feature snapshot into a clean clone after iCloud offloaded part of the local Git metadata; recovered files were checked against the original commit blobs before publication.
- GitHub Actions run `34158236017` completed successfully: both `Validate skill and examples` and `Package importable projects with published AIX` passed, including the locked preview gate.
- Designed and implemented the Japanese `skills/rokid-aiui-agent/assets/focus-timer-agent/` as a complete Page-only AIUI 0.17 Studio import root. It accepts integer `durationSeconds` from 1 through 3600 plus an optional 48-code-point `label`, and exposes `idle`, `running`, `paused`, `finished`, and `error` states.
- Added deterministic execution of the real Page script with fake `Date.now`, `setInterval`, and `clearInterval`. Coverage includes exact input bounds, malformed inputs, legal and invalid transitions, repeated actions, pause/continue, restart/reset, absolute-deadline completion, hide/show recalculation, and unload cleanup without wall-clock waits.
- Applied review fixes so each visible action owns its focus indication, running progress cannot display 100% before the deadline, all visible Page copy is Japanese, and new source files pass the tracked-whitespace gate.
- The local Homebrew Node 26 and npm installation was unhealthy (`node --version` and `npm --version` were terminated or stalled). Used the Codex-bundled Node.js 24.19.0 for tests/AIX and pnpm 11.19.0 to materialize the exact `@yodaos-pkg/aix-cli@0.8.2` dependency; no package manifest or lockfile version changed.
- Probed the installed AIX 0.8.2 help before execution; it advertised `preview`, `pack`, and `list`. The final focus-timer preview was 34,642 bytes and contained `pages/index/index.ink`; pack produced a 15,622-byte archive with SHA-256 `166756f6469a911c657c4be43c2fa5f525c7047fa57587f416d63cb0affa766a`, and list showed `AGENTS.md`, generated manifest, `VERSION`, `app.js`, `app.json`, and the declared Page.
- Browser automation could not open the local `file://` preview because of the browser URL security policy. Preview generation and content gates are verified, but visual rendering, authenticated AIUI Studio import, and physical Rokid Glasses behavior remain manual gates.

## 2026-09-09

- Added a RED/GREEN regression for the previously missing Focus Timer head-gesture path, then enabled Page World Awareness and routed only `nod` to the current primary action. Added a state-specific Japanese nod hint and explicitly documented that eye tracking is not used and focus/tap remains available.
- Exercised the real Page script with fake `Date.now`, `setInterval`, and `clearInterval`: World Awareness enables once, non-nod and missing events are ignored, idle starts, running pauses at the absolute remaining time, paused continues, finished restarts, and error remains unchanged.
- Ran the complete local suite with 62/62 passing tests and accepted `skills/rokid-aiui-agent/assets/focus-timer-agent/` with the strict AIUI `0.17.0` validator.
- Re-probed AIX 0.8.2 help using Node.js 24.19.0, then ran the advertised preview, pack, and list commands. Preview produced 36,302 bytes; pack produced 16,869 bytes with SHA-256 `85779702d65b2f59febbf346c7f486abb0e04d56828b2c2f93bab588b1856d99`; list contained the generated manifest, app files, Agent instructions, and `pages/index/index.ink`.
- Physical Rokid Glasses nod recognition, authenticated AIUI Studio import, and device rendering remain manual gates; local callback tests do not establish sensor behavior on hardware.
- Investigated the report that speaking “focus for 25 minutes” did not change the timer. Confirmed from the pinned AIUI 0.17 Page lifecycle that `onLoad(query)` runs once per Page instance, while the Agent instructions did not require a new Page call for later time changes.
- Added a RED/GREEN voice-reconfiguration contract: valid new or changed times now require a fresh Page invocation rather than a text-only reply, multilingual examples map 25 minutes to integer `durationSeconds: 1500`, and the Page tool description advertises both initial setup and changes. The real Page harness confirms a running 5-second instance can be replaced by a fresh 1500-second instance displaying `25:00` in `idle`.
- Re-ran AIX 0.8.2 help before its supported commands for the voice-routing revision. Preview produced 37,286 bytes; pack produced 17,607 bytes with SHA-256 `249d50994f95bca87459e644487a25bfef54008b56a856ff0b0850e7aea74b0b`; list showed `AGENTS.md`, generated manifest, `VERSION`, `app.js`, `app.json`, and `pages/index/index.ink`.
- Compared the timer input path with the pinned AIUI 0.17 conversational weather sample. Both use `schema.data` and receive fields directly through `onLoad(query)`, ruling out a nested-data hypothesis. Added Chinese time phrases to the Page tool description so tool selection does not depend only on Japanese instructions.
- Completed a functional and UX audit. Added a feature-detected World Awareness fallback so a missing host API cannot prevent Page load or button use, removed the non-functional reset action from the invalid-input state, raised the primary nod hint to 12px, and removed two `.DS_Store` files from the Studio import root.
- Generated and visually inspected the final 480x352 AIX preview: the Japanese error/recovery state was readable, monochrome green on black, and showed no dead action or clipping. The final preview was 37,454 bytes. The final explicit pack/list artifact was 17,732 bytes with SHA-256 `5b67a712815c75408ccd353c1dc4ed3decffa259775b0425b3eaa66164988a40` and contained only the expected Agent, manifest/version, app files, and Page.
- Found that public `origin/main` was still at `ce177a248349d1c2698c557c9d3e343c3fbc5d0c` and returned 404 for `skills/rokid-aiui-agent/assets/focus-timer-agent/`; the local branch contained the project but had never been published. GitHub Studio coordinates were therefore not yet usable, independent of voice parsing behavior.
- Published the complete Focus Timer commit series and audit fix to public `main` at `1b1ffeb5831ef972100e72c279d02f8f01f0f32a`. Verified the remote import root and inspected the remote Page content through the GitHub API. GitHub Actions run `34260945666` completed successfully for that feature commit.
- Added the requested 10-minute default through a RED/GREEN contract change. Missing, null, undefined, and label-only input now initialize an idle 600-second timer displaying `10:00`; explicitly supplied zero, out-of-range, fractional, string, or malformed-root input remains an error.
- Updated the Page schema with `default: 600` and made `durationSeconds` optional, while the Japanese Agent instructions still pass `durationSeconds: 600` when the user omits a time. Explicit speech such as `专注 25 分钟` continues to create a fresh Page with 1500 seconds.
- Ran the complete local suite with 62/62 passing tests, strict AIUI `0.17.0` validation, reference validation, and whitespace checks. After probing `aix --help` and adding the touchpad affordance, the advertised preview produced 38,086 bytes; pack produced 18,207 bytes with SHA-256 `972537f3a599509566e4a7a54a2208bdc15889823f44c2763a0bfc2d37f5815f`; list showed only the expected Agent, manifest/version, app files, and Page.
- Served the generated preview locally and visually confirmed a readable Japanese `10:00` idle state, black-and-green glasses canvas, start/nod hint, and no visible clipping. Authenticated Studio import and physical-glasses behavior remain manual gates.
- Made the glasses interaction affordance explicit in Japanese: nodding and a focused touchpad single-click both invoke the same state-specific primary action. Added a regression requiring the touchpad hint while retaining `bindtap`, `bindfocus`, and `bindblur` checks on every visible action.
- Added `.ink` and `.wxml` validator regressions for template directives inside closed XML/HTML comments and for conservative handling of unterminated comments. A mutation that disabled comment filtering caused both closed-comment cases to emit `WX_TEMPLATE_CONTROL_DIRECTIVE` and fail strict validation; restored code removes only explicitly closed comments, leaving unterminated content visible to directive and markup checks.
- Reproduced the reported click failure in the generated AIX simulator: clicking Enter left the Page at `10:00`, direction keys produced no element focus, and the console remained clean. The Page only handled `bindtap`, while CSS-hidden buttons had not entered the simulator's activatable focus tree.
- Compared the failure with the pinned AIUI 0.17 focus, Page event, and conditional-rendering documentation. A throwaway single-variable experiment showed that handling Page-level `onKeyUp(Enter)` immediately changed the primary action from start to pause; a separate `ink:if` experiment restored the missing visible buttons.
- Added RED/GREEN coverage for Enter and glasses-specific GlobalHook start/pause/continue routing, default-event prevention, ignored keys, error no-op behavior, official `ink:if` button mounting, and `_current` secondary-action suppression. The final simulator run visibly demonstrated start, pause, and continue through three successive Enter clicks.
- Re-ran all 62 unit tests successfully, accepted every fixture/example with its strict target-version validator, and passed Skill, reference, Python, Bash, YAML, and whitespace gates. After checking `aix --help`, the supported final preview produced 38,726 bytes; pack produced 18,687 bytes with SHA-256 `31e6b9dd50b22f920aa65eca4ddd31ae53551d0c155a7181a358be1a38db878c`; list contained only the expected Agent, manifest/version, app files, and Page.
## 2026-09-10

- Captured a focused RED behavior test for AIUI UX/capability sign-off. The pre-enhancement Skill correctly blocked release and found the described risks, but scored 8/12 with one critical artifact gap because separate project UX and per-capability evidence matrices were not mandatory.
- Rechecked primary upstream sources. Official AIUI `main` was `63bb5f5efa2f0c11a2defca35bc00777150f43d2`; its v0.18 `aiui-dev` checklist supplies general UX, capability, execution, and evidence discipline, while feature availability for stable projects remains pinned to `v0.17.0` at `88e70bb0382525c1a93ef077c2401dcc31a273ce`.
- Added an eighth routed reference, `ux-and-capability-testing.md`, and made its project-specific UX and per-capability matrices mandatory for every creation, implementation, code change, or review. Standardized results as `PASS`, `FAIL`, `BLOCKED`, or justified `N/A`, with six non-substitutable evidence layers from source review through physical glasses.
- Expanded the stable design reference with the official 16px horizontal and 12px vertical safe insets plus the 72% minimum green luminance for primary readable text. The source file is explicitly beta visual guidance present in the 0.17 tag, not proof of unrelated runtime features.
- The first enhanced-Skill forward response found the right risks and blocked release, but an independent review exposed a false-green score: its UX and capability tables had collapsed mandatory fields. Strengthened the behavior contract and evaluator instead of accepting the nominal 12/12 result.
- Ran all 69 then-current unit tests successfully with the bundled Node.js 24.19.0 after the system-default Node reproduced the repository's known `-9` termination. Passed OpenAI Skill validation, reference validation, Python compilation, Bash syntax, strict 0.17/0.18 validation for all fixtures/examples, and tracked-whitespace checks.
- Hardened the root Skill against short-report pressure: both matrices must retain their exact columns, and capability source cells require a commit-pinned official URL. The regression now validates row width, non-empty cells, allowed status values, named evidence layers, and pinned-source structure rather than checking headings alone.
- Added a second adversarial implementation-change scenario. The pre-enhancement Skill scored 2/7 and omitted both matrices; the hardened Skill scored 7/7, rejected evidence captured before the touch-to-voice change, and kept current-source, Studio, and physical-device work blocked.
- Re-ran the original audit with a fresh blind subagent after hardening. The final response scored 12/12, preserved the exact seven- and eleven-column schemas, recorded both known failures, and refused schedule-driven sign-off.
- A second independent review reproduced remaining evaluator bypasses: duplicate UX identifiers, fabricated or omitted evidence layers, evidence-free `PASS`, arbitrary or cross-version revisions, missing capability families, and contradictory release prose. Added RED mutations for each case, then made the evaluator parse exact layer tokens, require complete UX-family layer coverage, validate structured `PASS`/`N/A` evidence, bind 0.17/0.18 rows to allowlisted revisions and source paths, and require one four-field final decision.
- The first fully isolated post-review blind runs safely refused release but exposed another behavioral gap: they merged host and element focus, used umbrella inventory rows, and fell back to directory-level links for already named Button, CAMERA, voice, and head-gesture families. Added exact output headings, prohibited umbrella substitution, and routed those stable 0.17 families to narrow documentation and implementation/sample pairs while keeping genuinely unknown transports provisional and `BLOCKED`.
- Captured final content-verbatim blind transcripts after that hardening. The release-pressure audit scored 12/12 with the luminance-only UI and missing CAMERA recovery marked `FAIL`; the post-test touch-to-voice change gate scored 7/7 and rejected stale validator/unit/AIX evidence. Both had zero critical failures, retained the two exact matrices, and left Studio and physical-device checks blocked.

## 2026-09-10

- Replaced prose-only audit expectations with a fail-closed workflow bound to the exact AIUI Studio import root: deterministic source fingerprint, schema-2 capability inventory, closed `aiui-audit-claims.json`, exact supported-surface ledger, and one distinct gate for every discovered input path.
- Added a dependency-free audit validator with three stable outcomes: exit `0` only for a structurally valid release-ready `PASS`, exit `2` for a structurally valid `FAIL` or `BLOCKED` audit, and exit `1` for malformed, stale, tampered, or untrusted evidence. Source-unavailable reviews remain structurally auditable but can never become release-ready.
- Made all six evidence layers non-substitutable and content-addressed. Execution, Studio, device, and scope evidence now requires role-specific signatures verified by an absolute repository-external trust policy; canonical SPKI DER identities must be distinct across `RUNNER`, `STUDIO`, `DEVICE`, and `SCOPE`. Recorded `argv` is validated as provenance and is never executed by the validator.
- Added strict surface-aware `N/A` rules, current-source capture windows, exact runtime matching for Studio/device evidence, and a CI black-box test for the sole permitted release-ready exit-zero path.
- Hardened capability discovery through adversarial RED/GREEN tests: lexical `fetch` aliases and shadows, namespaced and unknown calls, Page/Widget/Worker lifecycle ownership, exact template bindings, `switch(event.code)` cases, regex/string/comment masking, `:host-focus`, camera/speech instance association, typed provisional inputs, and independent unresolved product claims.
- Closed product scope for every fixture, packaged asset, and generated example. Focus Timer explicitly declares fresh voice-triggered Page routing as unresolved host behavior; empty-scope projects carry an explicit closed empty ledger rather than relying on absence.
- Refreshed both adversarial behavior scenarios blindly. The UX/capability audit scored 12/12 and the post-change completion gate 7/7; both produced validator-accepted exit-2 blocked audits instead of fabricating Studio or device evidence.
- The pre-commit full suite intentionally reported 188/189 passing: the only failure proved that the newly added untracked claims ledger changed the exact fixture fingerprint relative to `HEAD`. This is expected to close only after the implementation and ledger are committed together, then the complete suite will be rerun from a clean clone before publication.
- Committed the implementation and ledgers together, then reran the complete suite successfully: 189/189 tests passed, including the 41-test UX/capability contract module and the signed-evidence release-ready black-box path. Skill structure validation, Python compilation, YAML parsing, whitespace checks, and both blind source-unavailable audits also passed with their expected semantics.
- Hardened the mandatory AIUI UX/capability gate after three independent adversarial reviews. The scanner now preserves Page/Widget/Worker ownership and lexical scope across regexes, loops, named expressions, Unicode escapes, generator callbacks, imported/local `wx`, TypeScript type-only regions, CSS/inline motion, indirect World Awareness calls, and repository-reserved references; ambiguous mechanisms are quarantined instead of silently accepted.
- Made repository scope explicit across fingerprint, inventory, strict project validation, audit reconciliation, and CI. Only top-level repository `.git/` and `.aiui-evidence/` aliases are excluded; nested same-named source remains fingerprinted, while evidence runtime source, symlinks, absolute/static references, and AIX package leakage are rejected.
- Closed UX applicability gaps: declared Page routes establish `Page`, exact `_current`/`_blank` targets remain separately required, every scanner input has an independent UX gate, recovery follows failure-capable families, Worker projects require a valid Page/Widget host, and shipped CSS/inline motion cannot be scoped to `N/A`.
- Re-ran the final local gate with the Codex-bundled Node.js 24.19.0 because the unrelated Homebrew Node process exits 137 on this host. All 257 tests passed; the audit-only module passed 57/57 and capability inventory module passed 69/69. OpenAI Skill validation, pinned-reference validation, Python/Bash/YAML syntax, whitespace checks, five strict Studio-import project validations, and the sole signed release-ready black-box path also passed.
- Published the verified feature snapshot to public `main` at `33dd2208774c19c5b979ee71f745e61c79f08a96`. GitHub Actions run `34425082574` passed both `Validate skill and examples` and `Package importable projects with published AIX`, including the sole release-ready black-box audit gate, all strict imports, all five package inspections, and all three previews.
- Defined a 17-test multilingual usage contract through RED/GREEN iteration, then added complete Chinese, English, and Japanese guides with shared language navigation and a root README entrypoint. Each guide covers installation, explicit invocation, five copyable development prompts, Studio-importable source output, clean-clone verification, mandatory UX/per-capability evidence, Studio import, completion semantics, and troubleshooting.
- Hardened the guides after independent review with a valid empty schema-1 claims ledger, deterministic verification order, explicit AIX help/preview/pack/list boundaries, exact audit exit-code mappings, and a clear rule that unexecuted Studio or physical-device gates remain `BLOCKED`.
- Applied the repository-scope decision before publication: moved Focus Timer into the installed Skill assets as the sole product Agent, removed unrelated Agent source/tests/design records from the current tree, and updated CI and all three usage guides to the new Studio import root.
- Completed the final pre-publication local gate with 275/275 tests passing under the bundled Node.js 24.19.0. Skill validation, reference validation, Python/Bash/YAML checks, whitespace checks, four strict Studio-import roots, the signed release-ready audit black-box path, three AIX pack/list flows, the Focus Timer static preview, and GitHub Skill publication dry-run all passed.
- Published the trilingual guides and repository-scope revision to public `main` at `185f7c4f2192ffced795091ee0cb8faac99af7bd`. GitHub Actions run `34440976706` completed successfully; the current public tree contains Focus Timer as the only bundled product Agent, under `skills/rokid-aiui-agent/assets/focus-timer-agent/`.
- Reorganized the root README as an approximately 100-line prompt-first Vibe Coding quickstart. Installation, a copyable standalone-project prompt, editable output, Studio import, Focus Timer, version behavior, and evidence boundaries now follow the reader's task; low-level audit and trust mechanics remain in the routed guides.
- Added RED/GREEN README contracts for a standalone output directory outside this repository, qualified Focus Timer voice behavior, Studio/device `BLOCKED` semantics, stable import coordinates, and absence of low-level audit terms. Focused documentation and timer tests passed 23/23, and independent specification and writing-quality reviews found no remaining Critical or Important issues.
- Completed the local publication gate with 276/276 tests passing under the bundled Node.js runtime. OpenAI Skill validation, pinned-reference validation, all four strict Studio-import roots, the signed release-ready audit black box, three real AIX pack/list flows, the Focus Timer static preview, and whitespace checks also passed.
- Closed the final independent review finding with a RED/GREEN contract: every project must unconditionally include all pages declared by `app.json.pages` and every resource those pages reference, including for single-page projects. The final reviewer returned `PASS`.
- Published the prompt-first README to public `main` at `4aa28ceb6ce05044fd226e4c2ca7f6f6f33e20f7`. GitHub Actions run `34449261892` passed both validation and AIX packaging/preview jobs; public API checks confirmed the README and Focus Timer import root, while isolated GitHub CLI and `npx skills` installs both delivered the complete Skill and bundled timer.
- Reviewed the private `goriri/rokid-aiui-demos` README as a non-normative information-architecture reference. Reused only its scannable presentation pattern: a capability overview, Focus Timer state-to-action table, and repository path-to-purpose map. No private screenshots, recordings, hosted services, performance figures, cloud/security claims, credentials, or license terms were copied or treated as evidence.
- Added semantic RED/GREEN README contracts for the three new sections, then closed independent review findings by converting the repository map to a table, telling newcomers to replace the example absolute output path, and introducing both task lists with concise prose. Final scope and writing reviews reported no Critical or Important findings.
- Ran the final local gate with the Codex-bundled Node.js runtime: 276/276 unit tests passed, as did Skill validation, reference validation, four strict AIUI import-root checks, the signed release-ready audit black box, three real AIX pack/list flows, the Focus Timer static preview, and whitespace checks. Studio import, voice routing, nod recognition, and touchpad behavior remain authenticated Studio or physical-device `BLOCKED` gates.
- Published the README feature snapshot to public `main` at `1af956d20c199dce145b489f644d32be955e21c6`. The first remote run (`34461428856`) exposed one design-document EOF whitespace error before tests began; reproduced it with CI's full-tree whitespace command, removed only the extra blank line, and retained the 23/23 focused regression result with the healthy Node.js runtime.
- GitHub Actions run `34461937056` then passed both `Validate skill and examples` and `Package importable projects with published AIX`. Public API reads confirmed the capability section, output-path replacement instruction, Focus Timer operation table, repository map, and complete Focus Timer import root on `main`.
- Defined the root README's two-route contract through RED/GREEN tests. The initial README failed only because it had no route subsections; adversarial review then hardened real H3 parsing, guarded primary/fallback installation, five ordered affirmative actions, CommonMark code-fence ownership, executed-check language, and negated required-object cases.
- Added a Studio-first route for opening an existing complete project or the bundled Focus Timer, plus a five-step Codex and Agent Skills-compatible Vibe Coding route for installing the Skill, working outside this repository, invoking `$rokid-aiui-agent`, checking the editable output and executed results, and importing the exact root into Studio. Independent scope, test-quality, and writing reviews found no remaining Critical or Important issues.
- Re-ran the complete local gate with Node.js 24.19.0: 276/276 unit tests passed, as did Skill and reference validation, four strict import-root checks, the signed release-ready audit black box, three real AIX pack/list flows, and both working-tree and committed-tree whitespace checks. The Focus Timer preview was 39,180 bytes and contained `pages/index/index.ink`; Studio and physical-device gates remain `BLOCKED`.
- Published the two-route README implementation to public `main` at `e99303f2815ac181dfbd7ec14f903234f67543c9`. GitHub Actions run `34498475242` passed both `Validate skill and examples` and `Package importable projects with published AIX`.
- Public GitHub API reads confirmed both route headings, all five Vibe Coding actions, explicit `$rokid-aiui-agent` invocation, the replaceable absolute output path, Studio handoff link, and the `BLOCKED` evidence boundary. The packaged assets remain only the Focus Timer Agent and the minimal template; the public timer root contains `AGENTS.md`, `aiui-audit-claims.json`, `app.js`, `app.json`, and `pages/index/index.ink`.
